name: GitHub Actions Sprint

on: push
jobs:
  builds:
      runs-on: windows-latest
      steps:
        - uses: actions/checkout@v3
        - name: GitHub Actions Sprint
          run: echo "This build successfully executed"
